---
name: 北市大畢業通
description: Dimension 入口與 Moksha 啟發的直向學分核對流程
colors:
  workspace-canvas: "#edf3f2"
  workspace-surface: "#f9fcfb"
  workspace-text: "#172e2c"
  workspace-accent: "#176752"
  dark-canvas: "#0c1116"
  dark-surface: "#152128"
  dark-text: "#ecf6f3"
  dark-accent: "#62e6c1"
  secondary-relief: "#eae5f4"
  dark-secondary-relief: "#29243b"
typography:
  display:
    fontSize: "clamp(2.2rem,5vw,4.2rem)"
    letterSpacing: "-.03em"
  headline:
    fontSize: "clamp(1.8rem,4vw,3rem)"
    fontWeight: 750
    lineHeight: 1.25
    letterSpacing: "-.025em"
rounded:
  control: "8px"
  expander: "12px"
  panel: "16px"
---

# Design System: 北市大畢業通

## Overview

工作區依使用者指定的 R3F Moksha 參考重建，以大章節標題、薄荷綠強調與連續捲動建立閱讀節奏。此版取代舊工作區的四入口橫排導覽及玻璃膠囊控制項規範。深度效果為原創 CSS 幾何與捲動視差，未實作原範例的 R3F、鑽石模型或 WebGL 場景。

`welcome.py` 的 Dimension 封面、攝影背景、透明細框入口與來源授權仍保留。工作區以 `workspace_ui.py`、`static/dimension/workspace.css`、`app.py` 與 `sidebar.py` 的實作為準；外觀更新不改功能及資料確認邊界。

## Colors

亮色使用淺灰綠畫布、近白表面與深綠文字；深色使用墨黑畫布、深青表面及明亮薄荷綠。章節標題與主要操作共用強調色。主修要求使用綠色浮雕表面，輔系及雙主修使用紫色，並保留身分文字標題。缺額與錯誤保留報告語意色與文字。

主題由 Streamlit 原生 Light／Dark／System 選項決定，主題橋接同步工作區與報告。沿用 `--ui-*` 及報告語意變數，同時處理背景、文字、邊線與控制項。

## Typography

工作區沿用 Noto Sans TC、Microsoft JhengHei 與系統無襯線字體。產品名稱為最大層級，章節標題採高字重、緊字距及強調色；內文與表單保留原生可讀性。Dimension 封面維持較開字距。

工作區不增加裝飾前導標；此版隱藏產品副標，保留課程名稱、數字、來源與待確認提示。

## Layout

工作區最大寬度（1160px），桌面內距（32px 56px 100px）。五個區段由上而下排列：本學期查課、修讀設定、匯入成績、成績一覽、畢業審核。沒有橫排章節導覽，也不以頁籤隱藏流程；個別表單仍可使用原生欄位排列。

首屏提供產品名稱及展開的查課功能。修讀設定未匯入時展開，已有成績時可收合。設定與匯入只在主頁渲染一份，側欄不建立重複表單。

章節上距（64px）、下距（24px）。窄於（640px）時頁面內距改為（16px 20px 60px），章節上距縮至（44px），前三區表面內距縮至（20px 16px），浮雕卡內距縮至（18px）。使用原生捲動，保留所有流程區段。

## Elevation & Depth

查課、設定與匯入採輕柔陰影；畢業要求保留雙向柔影的浮雕卡面。工具列與按鈕取消玻璃反光、模糊及位移，維持正常文件流程。

背景兩個細框幾何圖形不接收操作或輔助閱讀焦點。瀏覽器支援 scroll timeline 時才套用裝飾視差，不支援時維持靜態；表單不隨視差移動。手機及列印隱藏背景幾何，降低動態偏好停用幾何與報告動畫。缺額展開採（240ms）淡入及裁切揭示。

## Shapes

封面背景始終滿版、至少一個視窗高，不是圓角卡片。只有封面文字維持閱讀寬度；動畫中不得縮小封面。向下抵達工作區後收起封面，返回時先恢復再上滑；底部漸層銜接內頁底色，收起時維持內容視覺位置。

工作區控制項採小圓角，摺疊框與上傳框略圓，前三區表面及畢業要求卡片採一致的大圓角。上傳框使用虛線，摺疊框使用實線。Dimension 封面繼續使用原有細線及小圓角入口。

## Components

- 按鈕最小高度（46px），主要操作使用主題強調色，工具列採透明細框；滑入僅切換背景色，停用時降低透明度。鍵盤焦點使用（3px）外框與（4px）間距。
- 輸入與選單使用抬升畫布色，保留原生表單及禁用語意；摺疊標題最小高度（50px）。
- 修讀設定的連動欄位更新草稿，只有「套用設定」提交分析條件。
- 保留 PDF 匯入、選用校務登入與手動資料路徑。成績預覽不是畢業判定；只有明確確認的資料才進入分析，修改後必須重新確認。
- 畢業審核保留主修、輔系、雙主修要求、缺額課程、來源及報告匯出。未確認、缺資料及正式認定提醒保持可見。
- 封面保留在工作區上方。「開始檢查」與「返回封面」使用 850ms 單次上下捲動，不替換整個畫面；滾輪、觸控及鍵盤可中斷過渡，降低動態偏好直接定位。一般表單重跑不重播。
- 使用說明保留；封面往返不清除工作階段資料，個人成績不寫入封面或 URL。

## Do's and Don'ts

- **Do** 維持五個直向區段、明暗主題、原生操作及直白繁體中文。
- **Do** 將動態限定於背景與內容揭示，保持輸入位置穩定。
- **Don't** 恢復已取代的橫排章節導覽或工作區玻璃膠囊規範。
- **Don't** 將 CSS 視差描述成原 R3F Moksha 或鑽石 WebGL 的移植。
- **Don't** 為視覺效果更改學分規則、確認條件、數字或資料生命週期。
